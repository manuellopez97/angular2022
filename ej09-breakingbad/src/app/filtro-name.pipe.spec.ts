import { FiltroNamePipe } from './filtro-name.pipe';

describe('FiltroNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroNamePipe();
    expect(pipe).toBeTruthy();
  });
});
