import { QuoteNamePipe } from './quote-name.pipe';

describe('QuoteNamePipe', () => {
  it('create an instance', () => {
    const pipe = new QuoteNamePipe();
    expect(pipe).toBeTruthy();
  });
});
