import { CheaperThanPipe } from './cheaper-than.pipe';

describe('CheaperThanPipe', () => {
  it('create an instance', () => {
    const pipe = new CheaperThanPipe();
    expect(pipe).toBeTruthy();
  });
});
