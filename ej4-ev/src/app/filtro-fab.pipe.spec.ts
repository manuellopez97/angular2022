import { FiltroFabPipe } from './filtro-fab.pipe';

describe('FiltroFabPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroFabPipe();
    expect(pipe).toBeTruthy();
  });
});
